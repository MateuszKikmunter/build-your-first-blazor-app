@page "/todo"
@using FirstBlazorApp.Data

<h1>Todo</h1>

<ul>
    @foreach(var todo in todos)
    {
        <li>@todo.Title</li>
    }
</ul>

<input class="form-control" type="text" required placeholder="Something todo..." />
<button type="button" class="btn btn-outline-primary" @onclick="AddTodo">Add Todo</button>

@code {
    private IList<TodoItem> todos = new List<TodoItem>();

    private void AddTodo()
    {
        //Add todo item
    }
}